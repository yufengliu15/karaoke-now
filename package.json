{
  "name": "karaoke-now",
  "version": "0.1.0",
  "description": "Local-first karaoke practice app: vocal separation, synced lyrics, live pitch feedback",
  "main": "electron/main.js",
  "scripts": {
    "start": "electron .",
    "test": "node --test \"renderer/tests/*.test.mjs\"",
    "build:wasm": "asc wasm/yin.ts -o renderer/worklet/yin.wasm --optimizeLevel 3 --runtime stub --noAssert"
  },
  "license": "MIT",
  "devDependencies": {
    "assemblyscript": "^0.28.18",
    "electron": "^36.0.0"
  }
}
